import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Configurações padrão do Next.js. O plugin do Netlify (@netlify/plugin-nextjs)
  // cuida automaticamente do build e adaptações para a plataforma.
};

export default nextConfig;
